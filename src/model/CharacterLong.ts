export type Character = {
    id: number
    name: string
    image: string
    status: string
    origin: { name: string }
    location: { name:string }
    species: string
    type: string
    gender: string
}